<textarea id="{{ id }}" name="{{ name }}">{{ value|e('html') }}</textarea>

{% includejsresource 'ckeditor/ckeditor/ckeditor.js' %}
{% includejs %}CKEDITOR.replace('fields-{{ id|e('js') }}', {{ ckedtorConfig|json_encode|raw }}){% endincludejs %}
